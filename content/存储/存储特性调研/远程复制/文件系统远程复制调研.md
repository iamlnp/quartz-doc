# 概念

远程复制技术 则是远程容灾方案中的关键技术之一。远程复制(Remote Mirror)又称远程镜像，是数据镜像技术的一种，它能够在两个或多个 站点维护若干个数据副本，利用长距离来避免灾难发生时的数据丢失。

实现远程复制功能的技术较多，业界使用最为广泛的主要有同步远程复制和异步远程复 制两种。

- 同步远程复制：实时的同步数据，最大响度保证数据一致性，以减少灾难发生时的数据丢数量，属于同城灾备，对网络要求高，但数据丢失量可以为0。
- 异步远程复制：周期性地同步数据，最大限度的减少由于数据远程传输的时延而造成业务性能下降，属于异地灾备，对网络要求低，但复制距离没有限制。

**full copy(全备份)**：拷贝所有数据到目标中

**Increment copy(增量备份)**:仅仅将变化的数据拷贝到目标中

**Differential copy(差分备份)**：仅仅将新写的数据拷贝到目标中

**Synchronous(同步)**：写操作一直等待到，源端和目的端的写操作均被提交，写操作返回；或者源端和目的端之间具有关联性，比如：源端是目的端的一个指向列表，即源端删除文件，目的端也将删除文件。

**Asynchronous(异步)**：写操作仅仅等待源端中的写操作提交完成，写操作就返回，不关心目的端是否已经写完成。

**Fail Over(故障切换)**：当源端变为不可用时，数据和服务能够切换到目的端，从而替代源端，便于用户不间断的使用。这种形式下，数据读/写的方向都切换为目标端，源端变为不可达状态。

**Fail back(故障恢复）**：当源端变为可用时，能够将目的端数据恢复回源端，读写从主机切回到源端，源端将从目标端更新在Fail Over期间的数据。

# 竞品调研

## 华为OceanStor 18500

即华为HyperReplication业务，仅支持异步复制模式，可以实现周期性地同步数据，最大限度减少由于数据远程传输的时延而造成的业务性能下降。

### 原理描述

#### 相关概念

**远程复制租户Pair**：远程复制租户Pair是指本端存储系统的租户和远端存储系统的租户之间的远程复制关系

**远程复制Pair**：Pair是指主文件系统和从文件系统之间的数据复制关系。在文件系统远程复制中，数据只能由主文件系统经过链路复制到从文件系统。在进行数据复制之前，必须先建立主文件系统与从文件系统之间的Pair关系，正如在通话之前需要通过拨号建立两台电话之间的连接一样。在配置远程复制时，主端存储系统的一个主文件系统和从端存储系统的一个从文件系统构成一个Pair。

远程复制租户Pair与远程复制Pair关系：

![image-20220307200724320](image-20220307200724320.png)



**pair状态**

| 运行状态 | 状态说明                                                     |
| -------- | :----------------------------------------------------------- |
| 正常     | 主文件系统和从文件系统的数据成功同步完成。                   |
| 分裂     | “分裂”是指主从文件系统之间的数据暂停复制的一种Pair运行状态。由于业务需要，管理员手动断开主文件系统和从文件系统之间的Pair关系，则Pair运行状态为“分裂”。 |
| 异常断开 | 远程复制所用的链路断开或远程复制所用文件系统故障，导致主文件系统和从文件系统之间的Pair关系断开，则Pair运行状态为“异常断开”。 |
| 待恢复   | Pair异常断开，在故障恢复以后，如果需要以“手动”策略恢复远程复制，则Pair运行状态被标识为“待恢复”，提示用户需手动进行同步操作，恢复原有主文件系统和从文件系统的Pair关系。 |
| 失效     | Pair异常断开后，如果主文件系统或从文件系统原有的Pair属性改变（例如从站点的远程复制从文件系统被删除），导致主从站点配置不一致，则Pair运行状态为“失效”。 |
| 正在同步 | 主文件系统正在向从文件系统复制数据，此时，从文件系统的数据不可用。 |

**数据状态**：远程复制通过判断从文件系统在某一时间点数据是否可用，标识当前Pair的数据状态。当灾难发生时，远程复制需要根据Pair的数据状态判别是否允许进行主从切换。远程复制中Pair的数据状态为“完整”和“不完整”两种。

**Pair的同步**：是指将数据由主文件系统复制到从文件系统的过程。当主文件系统和从文件系统数据不一致时，可以通过手动同步操作，让主从远程复制对象数据保持一致。

- 全量复制
- 增量复制

**Pair的分裂**：分裂是指暂停由主文件系统向从文件系统同步数据。远程复制被分裂后，主从文件系统之间的Pair关系依然存在，此时主机对主从文件系统数据的读写权限并未改变。

对已经创建好的Pair，若需要暂时断开远程复制关系，例如，链路带宽不足，影响关键业务，需要暂停链路上远程复制数据的同步。此时，可以利用分裂操作来暂停数据的同步。

分裂和同步结合使用，可以对远程复制的数据同步过程进行有效的控制。

**Pair的主从切换**：主从切换是指Pair中文件系统的主从关系转换的过程。主从切换改变了远程复制中原有文件系统的主从关系，形成一个新的Pair。在远程复制中，主从切换通常应用于以下两种情况：

- 主站点灾难恢复后，重新建立远程复制链路，管理员首先在主站点或从站点对远程复制进行主从切换，当数据同步完成后，管理员再在主站点或者从站点对远程复制进行主从切换。
- 正常情况下需要对主端存储系统进行升级，例行维护等需要由从文件系统接管业务的操作，管理员在主站点或者从站点对远程复制进行主从切换。

主从切换的目的是为了在从端存储系统上运行业务，因此要求切换之前的从文件系统数据是可用的。通过查看从文件系统的数据状态来判别从文件系统数据是否可用。

**从文件系统可读写**：从文件系统可读写是指从文件系统可以接收来自主机的数据且自己的数据可以被主机读取。远程复制配置完成后从文件系统默认为只读，此时无需分裂可直接对从文件系统进行挂载，被挂载了的从文件系统可以被主机读取。如果主文件系统发生故障，主机的读业务可以由从文件系统承载，读业务不受影响。从文件系统可读常用于在复制过程中需要读取从端数据的场景，不适用于对数据一致性有严格要求的场景。

如果主文件系统出现故障，管理员需要手动设置从文件系统为可读写，以便从端存储系统能够接管主机业务，保证业务持续运行。设置从文件系统可读写的两种常见场景如下：

- 主文件系统故障，且远程复制链路断开，则需要在从端存储系统设置从文件系统可读写。
- 主文件系统故障，但远程复制链路正常，则需要先分裂Pair，然后在主端或从端存储系统设置从文件系统可读写。

### 数据复制

数据复制是指将主机产生的业务数据写入到从端存储系统的从文件系统中。当主文件系统和从文件系统建立文件系统远程复制关系之后，通常会启动初始同步，将主文件系统数据全部复制到从文件系统，以保证从文件系统数据和主文件系统在某一时刻（快照生成时）数据完全一致。

主端存储系统接收到主机发送的写I/O时，会发送写I/O至主文件系统，只要主文件系统返回写I/O成功，主端存储系统就会向主机返回写I/O成功。从文件系统的数据会在同步周期到达时，再进行主文件系统和从文件系统的数据同步。

![image-20220307191218505](image-20220307191218505.png)

![image-20220307191307393](image-20220307191307393.png)

DCL（data change log）:数据变化位图，疑问点：

- DCL与快照之间关系：差异日志中记录主文件系统上一次快照T1时刻之后变更数据的信息
- 同步的内容：将快照T2与前一个快照T1之间的差异数据复制至从端Cache

为了确保在系统故障时可以根据备份的快照快速恢复数据，默认情况下，主、从端集群都各自保留最新的两份快照。后续每次同步完成后，上一次同步完成后保存的两份快照中较早创建的一份快照将被删除，而保存最新同步所建立的快照。

### 业务切换（Failover）

当主站点遭受灾难，文件系统远程复制可以实现从站点迅速接管业务，保证业务持续运行。

文件系统远程复制不仅能够实现数据的远程备份，而且当灾难发生时，能够使业务在最短时间内得到恢复，以保证业务的持续运行。

- RPO（Recovery Point Objective）：是指在数据恢复中，能恢复出系统失败或灾难时的数据所需要的最小时间量。在数据损坏点之前这个时间段的数据都能被恢复。RPO表征业务可以容忍的数据丢失量。文件系统远程复制中RPO为设置的定时时长。
- RTO（Recovery Time Objective）是指把应用或关联的数据恢复到正确的操作状态所需要的最大时间。以恢复时间点为目标，确保备用主机能够快速接管业务。RTO表征业务可以容忍的中断时间。文件系统远程复制中RTO依赖于主机业务和故障场景。

能够在从端存储系统上运行业务的要求：

- 从端存储系统中存在历史业务数据。
- 备用主机上已配置与生产主机相同的业务应用。
- 从端存储系统支持主机对共享成功的文件系统进行读写操作。

![image-20220307193250964](image-20220307193250964.png)



### 故障恢复（Failback）

当主站点故障恢复后，需要将从站点的数据恢复到主站点中，从而让主站点重新对外提供服务。

![image-20220307193415737](image-20220307193415737.png)



## 曙光Parastor300

ParaStor300S 支持基于快照技术的异步远程复制。支持多套集群之间的目录级别复制，支持 1 对 1 复制、1 对 多复制(最大为 1:3)、多对 1 复制(最大为 32:1)、双向复制。

### 基本流程

1. 建立远程复制关系(复制 Pair)，确定参与复制的节点集合(复制区域)、复制链路(复制通路)、以及复制

   Pair 的本端目录(主站点)、远端目录(从站点)等;设置复制周期等策略。
   
2. 主站点数据一次性全复制到从站点，即初始同步。同步前，

   1. 在主站点目录创建快照，保留一致性点，获取本次快照和上次复制完成时快照之间的差异（全量复制时，直接扫描全体文件）
   2. 全量复制，从节点没有快照；增量复制，从节点存在上次复制完成时的快照
   3. 从节点存在快照时，需要回滚到上次的快照，避免脏数据对本次复制造成影响

3. 按照既定复制策略，周期性将增量数据同步到从站点，同步完成之后：

   1. 在从站点创建快照，用于下一次的增量复制。
   2. 从站点删除上一次同步结束时创建的快照
   3. 主站点删除上一次同步开始时创建的快照

4. 复制过程中故障导致中断，故障消除且系统恢复正常后，尚未复制的数据继续传输，即支持断点续传。

5. 从站点目录默认为只读，当主站点目录故障后，手动将前端业务切换到从站点，从而实现数据容灾保护。

### **概念解释**

- **复制域**：进行启动复制服务的区域，比如两套集群，每套集群有6个节点，集群A两个节点启动复制功能，集群B三个节点启动复制功能。
- **通道**：集群间的物理连接，指定对端设备ip，采用TCP协议，可以指定网卡、端口号等
- **pair**: 集群间的一条数据复制关系，描述数据复制的来源和目标，以及数据复制的频率等相关规则。创建pair时，需指定本端集群除根目录之外的任意目录作为数据复制的来源，病指定远端集群除根目录之外的任意空目录作为数据复制的目标。

### **产品特性**

- 支持全量复制和增量复制
- 支持断点续传
- 支持pair关系管理：增删改查以及分裂、恢复、主从切换
- 支持通道管理：增删改查以及指定网卡、端口等
- 支持pair任务管理：任务启动、cancel、暂停、继续、互斥、查询等，pair存在多种运行状态，比如normal、split、sync等等。
- 支持策略管理：增删改查，可以指定周期



# 需求点

复制内容：基本属性和扩展属性是否均支持复制，默认是否复制隐藏目录，需要区分.snapshot目录

分裂模式：主从之间pair可以删除，数据不能删除；分裂的pair，对端可以进行恢复；分裂完成后，复制策略及pair关系需要手动恢复

pair管理：创建pair时，对应的文件夹不允许嵌套，用户保证目标端存储路径的正确性；是否支持pair最高带宽限制；主从切换（切换后的相关行为）

策略管理：最小间隔时间；时间策略，比如每年/每月/每周/每日具体时间以及相关的增删改查

任务管理：定时启动/手动启动（启动互斥，同一对pair避免同时启动多次任务）；任务取消（是否回滚到上次一致性点）；任务失败的处理；同时启动多个任务的数量限制（不同pair）；手动/定时启动超任务限制时的区分处理；断点续传；任务查询包含的信息（进度、总文件数、已复制文件数、平均速率、开始时间、结束时间等等）；任务暂停与继续

传输处理：小文件聚合、传输路径选择、数据压缩

从目录写保护：是否支持；开启或取消写保护的联动行为（比如OceanStor 9000取消从目录写保护后会原从目录自动触发一次快照回滚，开启时新从目录也会触发一次回滚）

# 规格

| 项目                         | OceanStor 9000                                               | 曙光P300                                                     |
| ---------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 复制类型                     | 一对多复制（最大为1:3）<br />多对一复制（最大为32:1）<br />双向复制 | 1 对 多复制(最大为 1:3)<br />多对 1 复制(最大为 32:1)<br />双向复制 |
| 单个系统最大pair数           | 256                                                          | 4096                                                         |
| 单个系统最大同时运行的pair数 | 5                                                            | 8                                                            |
| 单个pair最大目录的文件数量   | 1亿                                                          | NA                                                           |
| 源目录下支持的路径最大长度   | 3400个字符                                                   | 256字符                                                      |
| 快照时长限制                 | 可配                                                         | 可配（但是有数量限制）                                       |

# 其他竞品

- EMC Isilon（OneFS）

# 参考文献

 [OceanStor V500R007 HyperReplication特性指南(文件业务).pdf](../白皮书/OceanStor V500R007 HyperReplication特性指南(文件业务).pdf)

https://support.huawei.com/enterprise/zh/doc/EDOC1000181460/ff925b76

 [OceanStor 9000 V5 7.1 文件系统特性指南 06.pdf](../../../work-management/设计文档/GP3.0/远程复制/OceanStor 9000 V5 7.1 文件系统特性指南 06.pdf) 

