---
写作年份: 2024
tags:
  - protobuf
---
1. CopyFrom： 
```c++
Tfs_SetAttrJob::handle_leader_prep() {
	...
	prep_req->mutable_cow_set()->mutable_srcpin_cow_set()->CopyFrom(*((const_cast<::Tfs::SetAttrRequest *>(request))->mutable_cow_set()->mutable_srcpin_cow_set()));
	...
}
```
2. mutable的直接赋值
```c++
Tfs_Client::__setattrx() {
	...
	if (!RP_out.snap.snap_set.empty()) {
		*(request.mutable_snaps()) = {RP_out.snap.snap_set.begin(), RP_out.snap.snap_set.end()};
	}
}
```